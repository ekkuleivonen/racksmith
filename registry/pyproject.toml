[project]
name = "registry"
version = "0.1.0"
description = "Racksmith role registry service"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]",
    "sqlalchemy[asyncio]",
    "asyncpg",
    "alembic",
    "httpx",
    "structlog",
]
