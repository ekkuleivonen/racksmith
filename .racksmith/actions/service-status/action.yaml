slug: service-status
name: Service status
description: Inspect the active state of a systemd service.
labels: [services, system]
inputs:
  - key: service_name
    label: Service name
    type: string
    placeholder: ssh
    default: ssh
    required: false
compatibility:
  os_family: []
